import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    txt: {
        fontSize: 40,
        color: '#ff442a',
        textAlign:'center'
    },
    btn: {
        justifyContent: 'center',
        width: 100,
        backgroundColor: '#ba6fff',
        marginTop: 30,

    }
});
export default styles